# ComputeTool

## Example Usage

```typescript
import { ComputeTool } from "@youdotcom-oss/sdk/models";

let value: ComputeTool = {
  type: "compute",
};
```

## Fields

| Field                                                                  | Type                                                                   | Required                                                               | Description                                                            |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| `type`                                                                 | *"compute"*                                                            | :heavy_check_mark:                                                     | Setting this value to "compute" is mandatory to use the compute agent. |