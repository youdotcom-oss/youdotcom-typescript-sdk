/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * SSE event signifying the response is starting
 */
export type ResponseStarting = {
  seqId: number;
  type: "response.starting";
};

/** @internal */
export const ResponseStarting$inboundSchema: z.ZodMiniType<
  ResponseStarting,
  unknown
> = z.pipe(
  z.object({
    seq_id: types.number(),
    type: types.literal("response.starting"),
  }),
  z.transform((v) => {
    return remap$(v, {
      "seq_id": "seqId",
    });
  }),
);

export function responseStartingFromJSON(
  jsonString: string,
): SafeParseResult<ResponseStarting, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ResponseStarting$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ResponseStarting' from JSON`,
  );
}
